const base = 'burger-constructor/';

export const BURGER_CONSTRUCTOR_ADD = `${base}add`;
export const BURGER_CONSTRUCTOR_DEL = `${base}del`;
export const BURGER_CONSTRUCTOR_MOVE = `${base}move`;
export const BURGER_CONSTRUCTOR_RESET = `${base}reset`;
